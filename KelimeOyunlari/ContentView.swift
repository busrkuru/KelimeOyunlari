import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hoşgeldiniz! Firebase ile giriş yaptınız.")
                .padding()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
